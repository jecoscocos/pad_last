# Микросервисная архитектура приложения для управления недвижимостью

## Описание проекта

Это приложение для управления недвижимостью, преобразованное из монолитной архитектуры в микросервисную. Система состоит из **12 независимых микросервисов** с чёткими границами ответственности.

## Архитектура

### Микросервисы (12 сервисов)

| № | Сервис | Порт | Описание |
|---|--------|------|----------|
| 1 | **api-gateway** | 5000 | Единая точка входа, UI и проксирование запросов |
| 2 | **auth-service** | 5001 | Аутентификация, JWT, управление пользователями |
| 3 | **property-service** | 5002 | Управление объектами недвижимости и фото |
| 4 | **inquiry-service** | 5003 | Заявки клиентов и планирование показов |
| 5 | **project-service** | 5004 | Проекты, задачи, комментарии |
| 6 | **search-service** | 5005 | Поиск и индексирование свойств |
| 7 | **notification-service** | 5006 | Уведомления (email/sms mock) |
| 8 | **analytics-service** | 5007 | Сбор аналитики (events, stats) |
| 9 | **reporting-service** | 5008 | Генерация отчётов |
| 10 | **payment-service** | 5009 | Обработка платежей (mock) |
| 11 | **media-service** | 5010 | Загрузка и обработка медиа-файлов |
| 12 | **logging-service** | 5011 | Централизованное логирование |

### 1. **Auth Service** (Port 5001)
**Ответственность:** Аутентификация и управление пользователями

**Функции:**
- Регистрация пользователей
- Аутентификация (JWT токены)
- Управление ролями (user/agent/admin)
- Валидация токенов для других сервисов

**API Endpoints:**
- `POST /register` - Регистрация нового пользователя
- `POST /login` - Вход в систему
- `POST /verify` - Проверка JWT токена
- `GET /users/{id}` - Получение информации о пользователе
- `GET /users` - Список всех пользователей

**База данных:** SQLite (auth.db)
- Таблица: User (id, email, password_hash, role, created_at)

---

### 2. **Property Service** (Port 5002)
**Ответственность:** Управление объектами недвижимости и фотографиями

**Функции:**
- CRUD операции с недвижимостью
- Загрузка и хранение фотографий
- Фильтрация по городу, типу, цене
- Доступ к фото через API

**API Endpoints:**
- `GET /properties` - Список объектов (с фильтрами)
- `GET /properties/{id}` - Детали объекта
- `POST /properties` - Создание объекта (только agent)
- `PUT /properties/{id}` - Обновление объекта (только agent)
- `DELETE /properties/{id}` - Удаление объекта (только agent)
- `GET /uploads/{filename}` - Получение фото

**База данных:** SQLite (property.db)
- Таблицы: Property, Photo

**Файловое хранилище:** /app/uploads (для фотографий)

---

### 3. **Inquiry Service** (Port 5003)
**Ответственность:** Обработка заявок клиентов и назначение встреч

**Функции:**
- Создание заявок на объекты
- Управление клиентской базой
- Планирование показов недвижимости
- Изменение статуса заявок (только agent)

**API Endpoints:**
- `POST /inquiries` - Создание заявки (публичный)
- `GET /inquiries` - Список заявок (с фильтрацией по роли)
- `GET /inquiries/{id}` - Детали заявки
- `PUT /inquiries/{id}/status` - Изменение статуса (только agent)
- `POST /appointments` - Создание встречи
- `GET /appointments` - Список встреч
- `GET /clients` - Список клиентов (только agent)

**База данных:** SQLite (inquiry.db)
- Таблицы: Client, Inquiry, Appointment

**Зависимости:**
- Auth Service - проверка токенов и ролей
- Property Service - проверка существования объектов

---

### 4. **Project Service** (Port 5004)
**Ответственность:** Управление проектами, задачами и комментариями

**Функции:**
- Создание проектов
- Управление задачами
- Комментирование задач
- Участники проектов

**API Endpoints:**
- `POST /projects` - Создание проекта
- `GET /projects` - Список проектов пользователя
- `GET /projects/{id}` - Детали проекта
- `DELETE /projects/{id}` - Удаление проекта (только owner)
- `POST /projects/{id}/tasks` - Создание задачи
- `GET /tasks/{id}` - Детали задачи
- `PUT /tasks/{id}` - Обновление задачи
- `POST /tasks/{id}/toggle` - Переключение статуса
- `DELETE /tasks/{id}` - Удаление задачи
- `POST /tasks/{id}/comments` - Добавление комментария

**База данных:** SQLite (project.db)
- Таблицы: Project, Task, Comment, ProjectMember

**Зависимости:**
- Auth Service - проверка токенов и идентификация пользователей

---

### 5. **API Gateway** (Port 5000)
**Ответственность:** Единая точка входа, маршрутизация, рендеринг UI

**Функции:**
- Обработка HTTP запросов от браузера
- Маршрутизация к соответствующим микросервисам
- Управление сессиями пользователей
- Рендеринг HTML шаблонов
- Агрегация данных из нескольких сервисов

**Технологии:**
- Flask для веб-сервера
- Session для хранения JWT токенов
- Jinja2 для шаблонов
- Requests для межсервисного взаимодействия

---

## Ключевые особенности микросервисной архитектуры

### ✅ Преимущества данной реализации:

1. **Разделение ответственности**
   - Каждый сервис имеет чёткую доменную границу
   - Независимые базы данных для каждого сервиса

2. **Независимое развертывание**
   - Каждый сервис в отдельном Docker контейнере
   - Можно обновлять сервисы независимо

3. **Масштабируемость**
   - Можно масштабировать сервисы по отдельности
   - Легко добавить load balancer для критичных сервисов

4. **Отказоустойчивость**
   - Healthcheck для каждого сервиса
   - Изоляция ошибок (падение одного сервиса не роняет систему)

5. **Технологическая гибкость**
   - Каждый сервис можно переписать на другой язык/фреймворк
   - Независимые зависимости (requirements.txt)

### 🔒 Безопасность:

- **JWT токены** для авторизации между сервисами
- **Проверка ролей** на уровне сервисов
- **Изолированная сеть** Docker для межсервисного взаимодействия
- **Валидация данных** на уровне каждого сервиса

### 🔄 Межсервисное взаимодействие:

- **Синхронное HTTP/REST** взаимодействие
- **Централизованная аутентификация** через Auth Service
- **Проверка зависимостей** (например, Inquiry проверяет существование Property)

---

## Требования

- Docker
- Docker Compose

## Запуск приложения

### 1. Перейти в папку microservices:
```bash
cd microservices
```

### 2. Собрать и запустить все сервисы:
```bash
docker-compose up --build
```

### 3. Доступ к приложению:
Откройте браузер и перейдите по адресу:
```
http://localhost:5000
```

### 4. Остановка приложения:
```bash
docker-compose down
```

### 5. Полная очистка (включая данные):
```bash
docker-compose down -v
```

---

## Порты сервисов

| Сервис | Порт | URL |
|--------|------|-----|
| API Gateway | 5000 | http://localhost:5000 |
| Auth Service | 5001 | http://localhost:5001 |
| Property Service | 5002 | http://localhost:5002 |
| Inquiry Service | 5003 | http://localhost:5003 |
| Project Service | 5004 | http://localhost:5004 |

---

## Тестирование API

### Создание пользователя (agent):
```bash
curl -X POST http://localhost:5001/register \
  -H "Content-Type: application/json" \
  -d '{"email":"agent@example.com","password":"password","role":"agent"}'
```

### Вход в систему:
```bash
curl -X POST http://localhost:5001/login \
  -H "Content-Type: application/json" \
  -d '{"email":"agent@example.com","password":"password"}'
```

### Создание объекта недвижимости (требует токен):
```bash
curl -X POST http://localhost:5002/properties \
  -H "Authorization: Bearer YOUR_JWT_TOKEN" \
  -F "title=Квартира в центре" \
  -F "city=Москва" \
  -F "address=ул. Пушкина, д. 10" \
  -F "price_eur=150000" \
  -F "property_type=apartment" \
  -F "rooms=3" \
  -F "area_m2=75" \
  -F "is_for_sale=true"
```

---

## Мониторинг здоровья сервисов

Каждый сервис имеет health endpoint:

```bash
curl http://localhost:5001/health  # Auth Service
curl http://localhost:5002/health  # Property Service
curl http://localhost:5003/health  # Inquiry Service
curl http://localhost:5004/health  # Project Service
```

Ожидаемый ответ:
```json
{"status": "healthy", "service": "service-name"}
```

---

## Структура проекта

```
microservices/
├── auth-service/
│   ├── app.py
│   ├── requirements.txt
│   └── Dockerfile
├── property-service/
│   ├── app.py
│   ├── requirements.txt
│   └── Dockerfile
├── inquiry-service/
│   ├── app.py
│   ├── requirements.txt
│   └── Dockerfile
├── project-service/
│   ├── app.py
│   ├── requirements.txt
│   └── Dockerfile
├── api-gateway/
│   ├── app.py
│   ├── requirements.txt
│   ├── Dockerfile
│   ├── templates/
│   └── static/
└── docker-compose.yml
```

---

## Роли пользователей

1. **user** (по умолчанию)
   - Просмотр объектов недвижимости
   - Создание заявок
   - Работа с проектами и задачами

2. **agent**
   - Все возможности user
   - Создание/редактирование/удаление объектов недвижимости
   - Просмотр всех заявок
   - Изменение статуса заявок
   - Создание встреч

3. **admin**
   - Все возможности agent
   - Управление пользователями

---

## Разработка

### Запуск отдельного сервиса локально:

```bash
cd auth-service
pip install -r requirements.txt
python app.py
```

### Переменные окружения:

Каждый сервис поддерживает следующие переменные:
- `SECRET_KEY` - секретный ключ для JWT
- `DATABASE_URL` - URL базы данных
- `PORT` - порт сервиса
- `*_SERVICE_URL` - URL других сервисов

---

## Отличия от монолитной архитектуры

| Аспект | Монолит | Микросервисы |
|--------|---------|--------------|
| **База данных** | Единая БД | 4 отдельные БД |
| **Развертывание** | Одно приложение | 5 независимых сервисов |
| **Масштабирование** | Весь монолит | Каждый сервис отдельно |
| **Технологии** | Единый стек | Гибкий выбор для каждого |
| **Аутентификация** | Flask-Login сессии | JWT токены |
| **Связность** | Прямые импорты | HTTP API вызовы |

---

## Troubleshooting

### Ошибка "Service unhealthy":
```bash
docker-compose logs service-name
```

### Пересоздание контейнеров:
```bash
docker-compose down
docker-compose up --build --force-recreate
```

### Проверка сети:
```bash
docker network ls
docker network inspect microservices_microservices-network
```

---

## Дальнейшие улучшения

1. **Message Queue** (RabbitMQ/Kafka) для асинхронных операций
2. **Service Discovery** (Consul/Eureka)
3. **API Documentation** (Swagger/OpenAPI)
4. **Централизованное логирование** (ELK Stack)
5. **Distributed Tracing** (Jaeger/Zipkin)
6. **Circuit Breaker** паттерн для отказоустойчивости
7. **PostgreSQL** вместо SQLite для production
8. **Redis** для кеширования
9. **Nginx** как reverse proxy
10. **Kubernetes** для оркестрации в production

---

## Автор

Лабораторная работа №2 - Микросервисная архитектура

## Лицензия

MIT License
