# Шпаргалка для защиты проекта

## Краткое описание

**Проект:** Веб-приложение "Агентство недвижимости"

**Две реализации:**
1. **Монолит** (корень проекта) — Flask приложение, SQLite, flask-login
2. **Микросервисы** (папка `microservices/`) — 12 независимых сервисов с Docker Compose

**Функционал:**
- Управление объектами недвижимости (CRUD)
- Загрузка фотографий
- Заявки клиентов и планирование показов
- Аутентификация и разделение ролей (user/agent/admin)
- Проекты и задачи
- Поиск, аналитика, отчёты, платежи, уведомления, логирование

---

## Технологический стек

| Компонент | Технология |
|-----------|-----------|
| Язык | Python 3.11+ |
| Фреймворк | Flask 3.1.2 |
| ORM | SQLAlchemy + Flask-SQLAlchemy |
| БД | SQLite (для демо) |
| Аутентификация | flask-login (монолит), JWT (микросервисы) |
| Контейнеризация | Docker + Docker Compose |
| Шаблоны | Jinja2 |
| Хеширование паролей | hashlib.sha256 (учебный вариант) |

---

## Архитектура микросервисов (12 сервисов)

| № | Сервис | Порт | Назначение |
|---|--------|------|------------|
| 1 | api-gateway | 5000 | Единая точка входа, UI |
| 2 | auth-service | 5001 | JWT аутентификация |
| 3 | property-service | 5002 | Недвижимость + фото |
| 4 | inquiry-service | 5003 | Заявки + встречи |
| 5 | project-service | 5004 | Проекты + задачи |
| 6 | search-service | 5005 | Поиск/индексирование |
| 7 | notification-service | 5006 | Email/SMS mock |
| 8 | analytics-service | 5007 | Аналитика событий |
| 9 | reporting-service | 5008 | Генерация отчётов |
| 10 | payment-service | 5009 | Обработка платежей |
| 11 | media-service | 5010 | Медиа-файлы |
| 12 | logging-service | 5011 | Централизованные логи |

**Взаимодействие:** HTTP REST, JWT токены для авторизации между сервисами.

---

## Быстрый запуск

### Монолит
```powershell
cd D:\Lab2Makeev
python -m venv .venv
.\.venv\Scripts\pip install -r requirements.txt
.\.venv\Scripts\python run.py
```
→ Открыть http://127.0.0.1:5000

### Микросервисы
```powershell
cd D:\Lab2Makeev\microservices
docker-compose up --build
```
→ Открыть http://localhost:5000

**Проверка health:**
```powershell
Invoke-RestMethod http://localhost:5001/health  # auth
Invoke-RestMethod http://localhost:5007/health  # analytics
```

---

## Основные модели (монолит)

**User:** id, email, password_hash, role  
**Property:** id, title, city, address, price_eur, property_type, rooms, area_m2, is_for_sale, is_for_rent  
**Photo:** id, property_id, file_path  
**Client:** id, name, email, phone  
**Inquiry:** id, property_id, client_id, name, email, phone, message, status  
**Appointment:** id, property_id, client_id, scheduled_at, note  
**Project/Task/Comment:** для управления проектами

---

## Возможные вопросы и ответы

### 1. Чем отличается монолит от микросервисов?
**Монолит:** Один процесс, одна БД, все модули в одном codebase. Простая разработка и deploy.  
**Микросервисы:** 12 независимых сервисов, каждый со своей БД и deployment. Масштабируемость, изоляция сбоев, но сложность выше.

### 2. Как реализована аутентификация?
**Монолит:** flask-login + sha256 хеш пароля (простое решение для демо).  
**Микросервисы:** JWT токены. auth-service выдаёт токен при логине, остальные сервисы проверяют через `/verify`.

### 3. Как сервисы общаются?
Через HTTP REST API (библиотека `requests`). Например, inquiry-service вызывает property-service для проверки существования объекта.

### 4. Почему SQLite, а не PostgreSQL?
SQLite — для демонстрации и быстрого прототипирования. В продакшене нужна PostgreSQL/MySQL для надёжности, параллелизма и масштабирования.

### 5. Как хранятся фотографии?
Локальная файловая система (`uploads/` в монолите, `/app/uploads` в property-service). В продакшене — S3/Azure Blob/GCS.

### 6. Что такое API Gateway?
Единая точка входа для клиентов. Роутит запросы к нужным микросервисам, отдаёт UI (шаблоны), управляет сессиями.

### 7. Как обеспечивается безопасность?
- JWT для межсервисной аутентификации
- Проверка ролей (agent/user)
- Хеш паролей (sha256 — учебный вариант; в продакшене нужен bcrypt/argon2)
- HTTPS (отсутствует в демо, нужно добавить)

### 8. Какие проблемы в текущей реализации?
- Слабое хеширование паролей (sha256 без соли)
- SQLite не для продакшена
- Нет rate limiting, CORS, мониторинга
- Секретные ключи хардкодед
- Отсутствие централизованного трейсинга

### 9. Как масштабировать приложение?
- Горизонтально: `docker-compose up --scale property-service=3` + load balancer
- Использовать Kubernetes
- Redis для кэша и сессий
- Message Queue (RabbitMQ/Kafka) для асинхронности

### 10. Какие дальнейшие улучшения?
- Заменить SQLite на PostgreSQL
- Внедрить bcrypt/argon2 для паролей
- Добавить Swagger/OpenAPI документацию
- Централизованный мониторинг (Prometheus + Grafana)
- CI/CD pipeline (GitHub Actions)
- Kubernetes манифесты
- Unit и integration тесты (pytest)

---

## Основные эндпоинты (примеры)

### Монолит
- `GET /` — главная/список объектов
- `POST /register` — регистрация
- `POST /login` — вход
- `GET /properties/new` — форма создания объекта
- `POST /properties/new` — создать объект
- `GET /properties/<id>` — детали объекта
- `POST /properties/<id>/inquiry` — отправить заявку

### Микросервисы (через API Gateway)
- `POST /register` → auth-service
- `POST /login` → auth-service
- `GET /` → property-service (список)
- `GET /search?q=apartment` → search-service
- `POST /notifications` → notification-service
- `GET /reports/properties` → reporting-service

---

## Команды для демонстрации

### Регистрация
```powershell
$body = @{ email='demo@test.com'; password='password123' } | ConvertTo-Json
Invoke-RestMethod -Method Post -Uri http://localhost:5001/register -Body $body -ContentType 'application/json'
```

### Логин и получение токена
```powershell
$body = @{ email='demo@test.com'; password='password123' } | ConvertTo-Json
$response = Invoke-RestMethod -Method Post -Uri http://localhost:5001/login -Body $body -ContentType 'application/json'
$token = $response.token
```

### Получить объекты
```powershell
Invoke-RestMethod http://localhost:5002/properties
```

### Создать событие аналитики
```powershell
$body = @{ event_type='page_view'; resource_id=1 } | ConvertTo-Json
Invoke-RestMethod -Method Post -Uri http://localhost:5007/events -Body $body -ContentType 'application/json'
```

### Получить статистику
```powershell
Invoke-RestMethod http://localhost:5007/stats
```

---

## Структура проекта

```
D:\Lab2Makeev/
├── app/                     # Монолит: models, routes, templates
├── run.py                   # Точка входа монолита
├── requirements.txt         # Зависимости монолита
├── microservices/           # Микросервисная версия
│   ├── api-gateway/
│   ├── auth-service/
│   ├── property-service/
│   ├── inquiry-service/
│   ├── project-service/
│   ├── search-service/
│   ├── notification-service/
│   ├── analytics-service/
│   ├── reporting-service/
│   ├── payment-service/
│   ├── media-service/
│   ├── logging-service/
│   ├── docker-compose.yml   # Оркестрация
│   └── README.md            # Подробная документация
└── DEFENSE_GUIDE.md         # Этот файл
```

---

## Ключевые термины для защиты

- **Монолит** — единое приложение, вся логика в одном процессе
- **Микросервисы** — независимые сервисы с чёткими границами
- **API Gateway** — единая точка входа для клиентов
- **JWT** — JSON Web Token для stateless аутентификации
- **REST API** — архитектурный стиль для HTTP API
- **Docker Compose** — оркестрация контейнеров
- **SQLite** — легковесная встраиваемая БД
- **Flask** — Python микрофреймворк для веба
- **ORM** — Object-Relational Mapping (SQLAlchemy)

---

## Полезные ссылки

- Документация Flask: https://flask.palletsprojects.com/
- Docker Compose: https://docs.docker.com/compose/
- Microservices pattern: https://microservices.io/
- JWT: https://jwt.io/

---

**Удачи на защите! 🚀**
