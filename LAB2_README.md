# Лабораторная работа №2 - Микросервисная архитектура

## 📋 Описание

Преобразование монолитного приложения для управления недвижимостью в микросервисную архитектуру.

**Исходное приложение:** Flask-приложение с единой базой данных  
**Результат:** 5 независимых микросервисов с чёткими границами ответственности

---

## 🏗️ Архитектура

### Микросервисы:

1. **Auth Service** (5001) - Аутентификация и пользователи
2. **Property Service** (5002) - Объекты недвижимости
3. **Inquiry Service** (5003) - Заявки и встречи
4. **Project Service** (5004) - Проекты и задачи
5. **API Gateway** (5000) - Единая точка входа

### Ключевые особенности:

✅ **Независимые базы данных** для каждого сервиса  
✅ **JWT токены** для межсервисной аутентификации  
✅ **Docker контейнеры** для изоляции сервисов  
✅ **Health checks** для мониторинга  
✅ **RESTful API** для взаимодействия  

---

## 🚀 Быстрый старт

### Требования:
- Docker Desktop
- Git

### Запуск:

```bash
# 1. Перейти в папку микросервисов
cd microservices

# 2. Запустить все сервисы
docker-compose up --build

# 3. Открыть браузер
http://localhost:5000
```

### Создание тестовых данных:

```bash
# В отдельном терминале (после запуска сервисов)
pip install requests
python seed_data.py
```

**Тестовые учетные данные:**
- Админ: `admin@test.com` / `admin123`
- Агент: `agent@test.com` / `agent123`
- Юзер: `user@test.com` / `user123`

---

## 📚 Документация

Подробная документация находится в папке `microservices/`:

- **[README.md](microservices/README.md)** - Полная документация
- **[QUICKSTART.md](microservices/QUICKSTART.md)** - Быстрый старт
- **[API.md](microservices/API.md)** - Описание API
- **[ARCHITECTURE.md](microservices/ARCHITECTURE.md)** - Диаграммы архитектуры

---

## 🔧 Структура проекта

```
Lab1Makeev/
├── app/                          # Старое монолитное приложение
│   ├── models.py
│   ├── routes.py
│   └── templates/
└── microservices/                # НОВАЯ микросервисная архитектура
    ├── auth-service/             # Сервис аутентификации
    │   ├── app.py
    │   ├── Dockerfile
    │   └── requirements.txt
    ├── property-service/         # Сервис недвижимости
    │   ├── app.py
    │   ├── Dockerfile
    │   └── requirements.txt
    ├── inquiry-service/          # Сервис заявок
    │   ├── app.py
    │   ├── Dockerfile
    │   └── requirements.txt
    ├── project-service/          # Сервис проектов
    │   ├── app.py
    │   ├── Dockerfile
    │   └── requirements.txt
    ├── api-gateway/              # API Gateway
    │   ├── app.py
    │   ├── Dockerfile
    │   ├── templates/
    │   └── static/
    ├── docker-compose.yml        # Оркестрация сервисов
    ├── README.md                 # Документация
    └── seed_data.py              # Скрипт тестовых данных
```

---

## 🎯 Основные отличия от монолита

| Характеристика | Монолит | Микросервисы |
|---------------|---------|--------------|
| **База данных** | 1 общая SQLite | 4 независимые БД |
| **Развертывание** | 1 приложение | 5 Docker контейнеров |
| | **Масштабирование** | Весь монолит | Каждый сервис отдельно |
| **Аутентификация** | Flask-Login (сессии) | JWT токены |
| **Связь компонентов** | Прямые импорты | HTTP/REST API |
| **Изоляция** | Общий процесс | Отдельные контейнеры |

---

## 📊 API Endpoints

### Auth Service (5001)
- `POST /register` - Регистрация
- `POST /login` - Вход
- `POST /verify` - Проверка токена

### Property Service (5002)
- `GET /properties` - Список объектов
- `POST /properties` - Создать объект
- `PUT /properties/{id}` - Обновить
- `DELETE /properties/{id}` - Удалить

### Inquiry Service (5003)
- `POST /inquiries` - Создать заявку
- `GET /inquiries` - Список заявок
- `POST /appointments` - Создать встречу

### Project Service (5004)
- `POST /projects` - Создать проект
- `POST /projects/{id}/tasks` - Создать задачу
- `POST /tasks/{id}/toggle` - Переключить статус

---

## 🔒 Безопасность

- **JWT токены** с истечением через 7 дней
- **Проверка ролей** на уровне сервисов
- **Изолированная сеть** Docker
- **Валидация данных** в каждом сервисе
- **Хеширование паролей** (SHA-256)

---

## 🧪 Тестирование

### Проверка здоровья сервисов:
```bash
curl http://localhost:5001/health  # Auth
curl http://localhost:5002/health  # Property
curl http://localhost:5003/health  # Inquiry
curl http://localhost:5004/health  # Project
```

### Просмотр логов:
```bash
docker-compose logs -f             # Все сервисы
docker-compose logs -f auth-service  # Конкретный сервис
```

---

## 🛠️ Команды

```bash
# Запуск
docker-compose up --build

# Остановка
docker-compose down

# Остановка с удалением данных
docker-compose down -v

# Перезапуск конкретного сервиса
docker-compose restart auth-service

# Пересборка
docker-compose up --build --force-recreate
```

---

## 📈 Преимущества микросервисной архитектуры

1. **Независимое развертывание** - обновляем только нужный сервис
2. **Масштабируемость** - увеличиваем реплики нагруженных сервисов
3. **Отказоустойчивость** - падение одного сервиса не роняет систему
4. **Технологическая гибкость** - разные сервисы на разных языках
5. **Команды** - разные команды работают над разными сервисами

---

## 🚧 Возможные улучшения

- [ ] Message Queue (RabbitMQ) для асинхронных операций
- [ ] PostgreSQL вместо SQLite
- [ ] Redis для кеширования
- [ ] Service Discovery (Consul)
- [ ] API Gateway на Nginx
- [ ] Kubernetes для оркестрации
- [ ] Swagger/OpenAPI документация
- [ ] Централизованное логирование (ELK)
- [ ] Distributed Tracing (Jaeger)
- [ ] Circuit Breaker паттерн

---

## 👨‍💻 Автор

**Makeev** - Лабораторная работа №2  
Дата: Октябрь 2025

---

## 📝 Лицензия

MIT License - используйте свободно для учебных целей

---

## 🆘 Поддержка

При возникновении проблем:

1. Проверьте логи: `docker-compose logs`
2. Убедитесь, что порты свободны
3. Попробуйте пересобрать: `docker-compose up --build --force-recreate`
4. Проверьте health endpoints всех сервисов
